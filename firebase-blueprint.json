{
  "entities": {
    "BrandingConfig": {
      "title": "BrandingConfig",
      "description": "App branding configuration managed by the reseller/owner",
      "type": "object",
      "properties": {
        "appName": { "type": "string", "description": "The custom app name" },
        "appSubtitle": { "type": "string", "description": "The custom subtitle" },
        "whatsAppLink": { "type": "string", "description": "Reseller WhatsApp channel link" },
        "pricingTier": { "type": "string", "description": "Pricing rate description" },
        "licenseSeed": { "type": "string", "description": "Secret validation seed key" },
        "payInfo": { "type": "string", "description": "Manual bank / QR payment info" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Timestamp when configuration was modified" }
      },
      "required": ["appName", "appSubtitle", "whatsAppLink", "pricingTier", "licenseSeed", "payInfo"]
    },
    "LicenseKey": {
      "title": "LicenseKey",
      "description": "Single-use license key for pro feature unlock",
      "type": "object",
      "properties": {
        "key": { "type": "string", "description": "Unique License Code formatted as MZPRO-XXXX-XXXX-XXXX" },
        "activated": { "type": "boolean", "description": "Active state verification flag" },
        "activatedBy": { "type": "string", "description": "Identifier tracking which user/device activated this key" },
        "activatedAt": { "type": "string", "format": "date-time", "description": "Timestamp tracking execution moment" },
        "duration": { "type": "string", "enum": ["30days", "unlimited"], "description": "Key subscription expiration validity duration type" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Key generation moment" }
      },
      "required": ["key", "activated"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "description": "Sync profile for user license keys & daily limits",
      "type": "object",
      "properties": {
        "email": { "type": "string", "description": "The user's account email address" },
        "displayName": { "type": "string", "description": "User's presentation display name" },
        "licenseKey": { "type": "string", "description": "The custom active license key string" },
        "trialStart": { "type": "string", "format": "date-time", "description": "The exact start moment of user's free trial" },
        "dailyUsage": { "type": "object", "description": "Day-by-day nested mapping tracking processing numbers per type" },
        "settings": { "type": "object", "description": "Nested configuration settings including API keys and preferences" },
        "promptGenHistory": { "type": "array", "description": "Array of saved text prompt generator logs" },
        "videoHistory": { "type": "array", "description": "Array of saved video keyword analysis logs" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Account cloud register moment" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Last synced moment" }
      },
      "required": ["email", "licenseKey"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "Individual chat message object, stored in either public channel or direct chat room",
      "type": "object",
      "properties": {
        "senderUid": { "type": "string", "description": "UID of the user sending the message" },
        "senderEmail": { "type": "string", "description": "Email address of the sender" },
        "senderName": { "type": "string", "description": "Display name of the sender" },
        "text": { "type": "string", "description": "Content of the text message" },
        "timestamp": { "type": "string", "format": "date-time", "description": "Moment of creation generated by database" }
      },
      "required": ["senderUid", "senderEmail", "text"]
    },
    "ChatRoom": {
      "title": "ChatRoom",
      "description": "Information tracking a direct messaging peer thread",
      "type": "object",
      "properties": {
        "user1": { "type": "string", "description": "Uid representing peer one" },
        "user2": { "type": "string", "description": "Uid representing peer two" },
        "user1Email": { "type": "string", "description": "Email of user one" },
        "user2Email": { "type": "string", "description": "Email of user two" },
        "user1Name": { "type": "string", "description": "Name of user one" },
        "user2Name": { "type": "string", "description": "Name of user two" },
        "lastMessage": { "type": "string", "description": "Excerpt of most recent chat message" },
        "lastMessageTime": { "type": "string", "format": "date-time", "description": "Latest update timestamp of room" }
      },
      "required": ["user1", "user2"]
    },
    "PromoCode": {
      "title": "PromoCode",
      "description": "Marketing promotion and discount vouchers",
      "type": "object",
      "properties": {
        "code": { "type": "string", "description": "Voucher identifier such as PROMO50" },
        "type": { "type": "string", "enum": ["free_premium", "discount"], "description": "Type of benefits offered" },
        "value": { "type": "number", "description": "Discount percent number or days counter for premium" },
        "maxUses": { "type": "number", "description": "Highest allows redeem count" },
        "usedCount": { "type": "number", "description": "Counter indicating total successfully claimed events" },
        "description": { "type": "string", "description": "Marketing details, notes, or titles" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Moment of creation" }
      },
      "required": ["code", "type", "value", "maxUses", "usedCount"]
    },
    "FeedbackMessage": {
      "title": "FeedbackMessage",
      "description": "User suggestions and troubleshooting requests",
      "type": "object",
      "properties": {
        "message": { "type": "string", "description": "The description written by the user" },
        "timestamp": { "type": "string", "format": "date-time", "description": "When the feedback was sent" },
        "userEmail": { "type": "string", "description": "Email address of the sender" }
      },
      "required": ["message", "timestamp", "userEmail"]
    }
  },
  "firestore": {
    "/branding/{brandingId}": {
      "schema": "BrandingConfig",
      "description": "Centralized branding rules. We will use a singleton document ID 'main' to represent the active config."
    },
    "/keys/{keyId}": {
      "schema": "LicenseKey",
      "description": "Unique key objects. Document ID matches the generated key string itself."
    },
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Individual account state, synced to cloud."
    },
    "/global_messages/{messageId}": {
      "schema": "ChatMessage",
      "description": "Public chat messages visible to all registered accounts."
    },
    "/chats/{chatId}": {
      "schema": "ChatRoom",
      "description": "Individual DM threads established between accounts."
    },
    "/chats/{chatId}/messages/{messageId}": {
      "schema": "ChatMessage",
      "description": "Specific messages inside a direct peer-to-peer chat thread."
    },
    "/promos/{promoId}": {
      "schema": "PromoCode",
      "description": "Promo codes created by reseller/admin for discounts or free premium access."
    },
    "/feedback/{feedbackId}": {
      "schema": "FeedbackMessage",
      "description": "User feedback reports and suggestions."
    }
  }
}
